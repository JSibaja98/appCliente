import 'package:flutter/material.dart';

class BooksStorePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Books store'),
    );
  }
}